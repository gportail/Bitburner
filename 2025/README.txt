Alias :
alias s="cls; run stats.js"
alias sa="cls; run stats.js -a -o"
alias so="cls; run stats.js -o"
alias -g h="home"
alias -g l="ls"
alias -g nuke="run NUKE.exe"
alias -g brutessh="run BrusteSSH.exe"
alias -g s3="scan-analyze 3"
alias -g s5="scan-analyze 5"
alias -g s10="scan-analyze 10"
alias witch="run witch.js"

Step 1: deploiement initial :
  run autohack.js
  run deploy.js
  run moniteur.js
  lancer "hack.js" sur le serveur qui a le plus gros "Hack $"
  Acheter des node hacknet
  lancer la compilation de "BruteSSH.exe"
  lancer la compilation de "FTPCrack.exe"
  
Step 2:
  Acheter des serveur avec 8go RAM
  run deploy.js
  lancer "deploy_w_and_g.js" avec comme cible le serveur qui a le plus gros "Hack$/s"
  lancer "hack.js" avec comme cible le serveur qui a le plus gros "Hack$/s"


Ordre des factions:
  1 - Tian Di Hui
    $1,000m
    Hacking Level 50
    Be in Chongqing, New Tokyo, or Ishima
    => Neuroreceptor Management Implant
    => Social Negotiation Assistant (S.N.A) *

  2 - Sector-12
    Be in Sector-12
    $15,000m
    Not in Factions: Chongqing, New Tokyo, Ishima, Volhaven
    => CashRoot Starter Kit : Start with $1m after installing Augmentations. Start with BruteSSH.exe after installing Augmentations.

  3 - CyberSec
    Install a backdoor on the CSEC server